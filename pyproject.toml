[project]
name = "pulse-ui-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pulse>=0.1.0",
]


[tool.uv.sources]
pulse = { git = "https://github.com/erwinkn/pulse-ui" }
